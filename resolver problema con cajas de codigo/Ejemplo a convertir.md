Las siguientes son las instrucciones para instalar el programa

```
sudo apt update
sudo apt install cmake build-essential libqt5x11extras5-dev \
    qt5-qmake dh-make qtbase5-dev-tools \
    extra-cmake-modules
```

con eso podrá funciona bien.
